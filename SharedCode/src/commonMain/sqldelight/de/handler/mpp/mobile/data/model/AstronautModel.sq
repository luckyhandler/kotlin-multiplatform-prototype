CREATE TABLE IF NOT EXISTS astronaut (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  craft TEXT NOT NULL,
  name TEXT NOT NULL
);

insertItem:
INSERT INTO astronaut(craft, name) VALUES (?, ?);

selectAll:
SELECT *
FROM astronaut;

